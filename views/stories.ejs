<!--goto http://www.cookiesound.com/ for inspiration-->
<link rel="stylesheet" href="/css/stories.css" />
<div id="stories" class="stories">
  <!-- TODO: Image Positioning -->
  <div
    id="bgImage"
    class="bgImage"
    style="background: url(<%= fileurl %>) no-repeat;"
  ></div>
  <div id="logo">
    <h2>.xylo</h2>
  </div>
  <div class="heading">
    <h1 class="title"><%= obj.print[0].title %></h1>
  </div>

  <div class="content">
    <span id="date"><%= obj.print[0].date %></span>&nbsp;
    <span>✦</span>
    <span id="place"><%= obj.print[0].place %></span>
    <h1 id="title"><%= obj.print[0].title %></h1>
    <p id="story">
      <%= obj.print[0].content %>
    </p>
    <br />
    <p>
      _______________________________________________________________________________________
    </p>
  </div>
  <div class="comments">
    <span id="Heading">Comments</span> <br />
    <% if(comment.length > 0) { for(let i = 0; i < comment.length; i++) { var
    element = comment[i]; %>
    <br>
    <label><%= element.person_name %></label>
    <br />
    <label><%= element.content %></label>
    <% }}else{ %> <br>No comments yet... <% } %>
    <br />

    <% if(uid != undefined) { %>
    <form method="POST">
      <br>
      <input type="hidden" name="post_id" value="<%= obj.print[0].id %>"/>
      <br>
      <label>Add a comment:</label>
      <br />
      <textarea
        id="content-comment"
        cols="100"
        rows="2"
        maxlength="140"
        name="content"
      ></textarea>
      <script>
        content.oninput = function() {
          remaining.innerHTML =
            140 - content.value.length + " characters remaining...";
        };
      </script>
      <br />
      <label id="remaining"></label>
      <br />
      <input id="submit-button" type="submit" value="Add comment" />
    </form>
    <% } %>
  </div>
</div>
